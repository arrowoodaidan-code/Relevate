node_modules

.env
.env.local
.DS_Store
.cache
.output
.nitro
.tanstack

/dist/
/dist-ssr/

# Vercel Build Output API bundle (produced by build-vercel.sh at go-live).
/.vercel/

# Runtime server log written on start.
/.run/

# Generated by the TanStack Start router plugin on dev/build.
src/routeTree.gen.ts
.vercel
# Env files are local only: no .env* file may be tracked. Real values arrive as
# environment variables; scripts/check-no-committed-secrets.ts enforces this.
.env*
